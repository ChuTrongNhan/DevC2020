import React from "react";
import { StyleSheet, View, Text } from "react-native";

const ActiveTodoScreen = (props) => {
  return (
    <View>
      <Text>Active</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ActiveTodoScreen;
