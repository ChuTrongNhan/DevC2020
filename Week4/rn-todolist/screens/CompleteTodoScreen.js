import React from "react";
import { StyleSheet, View, Text } from "react-native";

const CompleteTodoScreen = (props) => {
  return (
    <View>
      <Text>Complete</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CompleteTodoScreen;
